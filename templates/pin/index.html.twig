{% extends 'base.html.twig' %}

{% block title %}Hello PinController!{% endblock %}

{% block body %}

<h1>{{ pluralize(pins|length, 'Pin') }}</h1>

{% for pin in pins %}
<article>
<h4>{{pin.title}}</h4>
<p>{{pin.description}}</p>
</article>
    
{% endfor %}

{% endblock %}
