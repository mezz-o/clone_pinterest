
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
{{ form_widget(form) }}
<br />
<input class='btn-grad btn-block' type="submit" value="{{ submitText ?? 'Create Pin' }}">

{{ form_end(form) }}