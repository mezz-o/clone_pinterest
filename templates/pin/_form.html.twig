
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
{{ form_widget(form) }}

<input class='btn btn-primary btn-block' type="submit" value="{{ submitText ?? 'Create Pin' }}">

{{ form_end(form) }}